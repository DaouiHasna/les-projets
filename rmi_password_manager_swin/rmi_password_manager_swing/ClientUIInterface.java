/**
 * A simple interface for the client UI to display messages or errors.
 */
public interface ClientUIInterface {
    /**
     * Display an informational or error message to the user (e.g., in a popup or status label).
     */
    void showMessage(String message);
}
